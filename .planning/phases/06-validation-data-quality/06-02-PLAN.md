---
phase: 06-validation-data-quality
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/exporter.py
  - main.py
  - tests/test_exporter.py
autonomous: true
requirements:
  - STAB-07

must_haves:
  truths:
    - "summary.json contém o campo total_itens na raiz do JSON com valor inteiro"
    - "summary.json contém o campo cobertura_bps_pct na raiz do JSON com valor float arredondado a 1 casa"
    - "summary.json contém o campo alertas_por_tier na raiz do JSON como dict com contagens por valor de nivel_alerta"
    - "Pipeline imprime linha no formato 'Items: N | BPS match: X% | No match: Y%' ao final do resumo"
    - "Pipeline emite [WARNING] quando cobertura_bps_pct < 50"
    - "Todos os testes existentes de TestGerarSummary continuam passando (zero regressões)"
  artifacts:
    - path: "src/utils/exporter.py"
      provides: "gerar_summary() com os 3 novos campos na raiz do dict"
      contains: "total_itens"
    - path: "main.py"
      provides: "Bloco de resumo expandido com linha BPS match e WARNING condicional"
      contains: "BPS match"
    - path: "tests/test_exporter.py"
      provides: "3 novos testes em TestGerarSummary cobrindo total_itens, cobertura_bps_pct, alertas_por_tier"
      contains: "total_itens"
  key_links:
    - from: "src/utils/exporter.py:gerar_summary()"
      to: "docs/summary.json"
      via: "json.dumps() na linha final do método"
      pattern: "total_itens.*cobertura_bps_pct.*alertas_por_tier"
    - from: "main.py bloco de resumo"
      to: "docs/summary.json"
      via: "json.load() lendo summary_file"
      pattern: "cobertura_bps_pct"
    - from: "tests/test_exporter.py:TestGerarSummary"
      to: "src/utils/exporter.py:gerar_summary()"
      via: "instância de Exporter e fixture de alertas CSV com nivel_alerta"
      pattern: "test_total_itens\|test_cobertura_bps\|test_alertas_por_tier"
---

<objective>
Estender gerar_summary() em src/utils/exporter.py com 3 novos campos no summary.json e expandir o bloco de resumo final em main.py com observabilidade de cobertura BPS.

Purpose: O dashboard e o pipeline carecem de métricas de qualidade de dados — não há como saber quantos itens têm preço de referência BPS disponível. Esta extensão cirúrgica adiciona total_itens, cobertura_bps_pct e alertas_por_tier ao summary.json, e imprime a linha "Items: N | BPS match: X% | No match: Y%" no terminal ao final de cada execução. (STAB-07)

Output:
  - src/utils/exporter.py — gerar_summary() com 3 novos campos
  - main.py — bloco de resumo expandido com linha BPS match + WARNING
  - tests/test_exporter.py — 3 novos testes em TestGerarSummary (+ 1 para DataFrame vazio)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@CLAUDE.md

<interfaces>
<!-- Interfaces extraídas de src/utils/exporter.py e main.py -->

Assinatura atual de gerar_summary() (src/utils/exporter.py linha 106):
```python
def gerar_summary(
    self,
    alertas_file: str,
    output_file: str,
    ano: int = 2023,
    fornecedores_file: Optional[str] = None,
) -> dict:
```

Estrutura atual de summary dict (src/utils/exporter.py linhas 219-235):
```python
summary = {
    "gerado_em": datetime.now().isoformat(),
    "municipio": "Sorocaba",
    "ano": ano,
    "periodo_analise": {"inicio": inicio, "fim": fim},
    "totais": {
        "empenhos_analisados": total_empenhos,
        "alertas_atencao": alertas_atencao,
        "alertas_alerta": alertas_alerta,
        "alertas_criticos": alertas_criticos,
        "valor_total_excedente": valor_total,
        "fornecedores_suspeitos": qtd_fornecedores_suspeitos,
    },
    "top_itens": top_itens,
    "top_fornecedores": top_fornecedores,
    "distribuicao_alertas": distribuicao,
}
```

Distribuição calculada logo acima do dict (linhas 213-217):
```python
distribuicao = {
    "ATENÇÃO": alertas_atencao,
    "ALERTA": alertas_alerta,
    "CRÍTICO": alertas_criticos,
}
```

Bloco de resumo em main.py (linhas 184-195) — bloco a ser expandido:
```python
# Mostrar resumo no terminal (lê de docs/summary.json se disponível)
summary_file = ROOT / "docs" / "summary.json"
if not summary_file.exists():
    summary_file = output_path / "summary.json"
if summary_file.exists():
    with open(summary_file) as f:
        s = json.load(f)
    totais = s.get("totais", {})
    console.print(f"\n[bold]Resumo:[/bold]")
    console.print(f"   Empenhos analisados: {totais.get('empenhos_analisados', '?')}")
    console.print(f"   Alertas CRÍTICOS: {totais.get('alertas_criticos', '?')}")
    console.print(f"   Valor total excedente: R$ {totais.get('valor_total_excedente', '?')}")
```

Testes existentes em TestGerarSummary (tests/test_exporter.py linha 373+):
- test_top5 (linha 375)
- test_top5_menos_que_5 (linha 394)
- test_schema_totais (linha 431) — verifica chaves de "totais" — NÃO tocar neste teste
- test_salva_arquivo (linha 454)
- test_summary_json_sem_nan (linha 473)
- test_nivel_risco_suspeito (linha 507)

Semântica de nivel_alerta (VERIFIED: src/analyzers/price_analyzer.py):
- "SEM_REFERÊNCIA" → item sem preço BPS disponível (preco_mediano is None)
- "OK" → preço dentro do limiar
- "ATENÇÃO", "ALERTA", "CRÍTICO" → tiers de alerta crescente
- Todos os valores exceto "SEM_REFERÊNCIA" têm preço BPS disponível

Cálculo de cobertura BPS (da pesquisa — VERIFIED: price_analyzer.py linhas 246-260):
```python
total_itens = len(alertas_df)
if not alertas_df.empty and "nivel_alerta" in alertas_df.columns:
    sem_ref = int((alertas_df["nivel_alerta"] == "SEM_REFERÊNCIA").sum())
    com_bps = total_itens - sem_ref
    cobertura_bps_pct = round((com_bps / total_itens) * 100, 1) if total_itens > 0 else 0.0
    alertas_por_tier = {
        k: int(v) for k, v in alertas_df["nivel_alerta"].value_counts().items()
    }
else:
    cobertura_bps_pct = 0.0
    alertas_por_tier = {}
```

Pitfall crítico: rich.Console NÃO é capturado por capsys no pytest.
Solução: Testes verificam os campos do summary.json, não o stdout.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Adicionar testes para os 3 novos campos em TestGerarSummary</name>

  <read_first>
    tests/test_exporter.py
    src/utils/exporter.py
  </read_first>

  <behavior>
    - Test 1 (test_total_itens): fixture com 5 alertas de diferentes tiers → summary["total_itens"] == 5
    - Test 2 (test_cobertura_bps_pct): fixture com 4 alertas (2 SEM_REFERÊNCIA, 2 outros) → summary["cobertura_bps_pct"] == 50.0
    - Test 3 (test_alertas_por_tier): fixture com tiers variados → summary["alertas_por_tier"] é dict com chaves correspondendo aos valores de nivel_alerta presentes
    - Test 4 (test_cobertura_bps_df_vazio): DataFrame vazio → summary["cobertura_bps_pct"] == 0.0 e summary["alertas_por_tier"] == {}
    - Os campos devem estar na RAIZ do dict, NÃO dentro de "totais"
    - Todos os testes existentes de TestGerarSummary devem continuar passando
  </behavior>

  <action>
Abrir `tests/test_exporter.py` e adicionar os quatro métodos ao final da classe `TestGerarSummary`
(após o último método existente `test_nivel_risco_suspeito`).

Inserir APENAS os novos métodos — não modificar nenhum método existente.

Os quatro métodos a adicionar:

```python
def test_total_itens(self, alertas_csv_fixture, tmp_path):
    """total_itens deve conter o número total de linhas no CSV de alertas."""
    exporter = Exporter()
    out = tmp_path / "summary.json"
    result = exporter.gerar_summary(
        alertas_file=str(alertas_csv_fixture),
        output_file=str(out),
        ano=2023,
    )
    assert "total_itens" in result, "Campo total_itens ausente na raiz do summary"
    assert isinstance(result["total_itens"], int), "total_itens deve ser int"
    assert result["total_itens"] > 0

def test_cobertura_bps_pct(self, tmp_path):
    """cobertura_bps_pct deve ser float entre 0 e 100 representando % com match BPS."""
    import io
    # Criar CSV com 4 alertas: 2 SEM_REFERÊNCIA, 1 ATENÇÃO, 1 CRÍTICO
    csv_content = (
        "nivel_alerta,valor_excedente_total,descricao_item\n"
        "SEM_REFERÊNCIA,0.0,Medicamento A\n"
        "SEM_REFERÊNCIA,0.0,Medicamento B\n"
        "ATENÇÃO,500.0,Medicamento C\n"
        "CRÍTICO,1000.0,Medicamento D\n"
    )
    alertas_path = tmp_path / "alertas.csv"
    alertas_path.write_text(csv_content, encoding="utf-8")
    out = tmp_path / "summary.json"
    exporter = Exporter()
    result = exporter.gerar_summary(
        alertas_file=str(alertas_path),
        output_file=str(out),
        ano=2023,
    )
    assert "cobertura_bps_pct" in result, "Campo cobertura_bps_pct ausente na raiz do summary"
    assert isinstance(result["cobertura_bps_pct"], (int, float)), "cobertura_bps_pct deve ser numérico"
    assert result["cobertura_bps_pct"] == 50.0, f"Esperado 50.0, obtido {result['cobertura_bps_pct']}"

def test_alertas_por_tier(self, tmp_path):
    """alertas_por_tier deve ser dict com contagem por valor de nivel_alerta."""
    csv_content = (
        "nivel_alerta,valor_excedente_total,descricao_item\n"
        "SEM_REFERÊNCIA,0.0,Med A\n"
        "ATENÇÃO,100.0,Med B\n"
        "ATENÇÃO,200.0,Med C\n"
        "CRÍTICO,900.0,Med D\n"
    )
    alertas_path = tmp_path / "alertas.csv"
    alertas_path.write_text(csv_content, encoding="utf-8")
    out = tmp_path / "summary.json"
    exporter = Exporter()
    result = exporter.gerar_summary(
        alertas_file=str(alertas_path),
        output_file=str(out),
        ano=2023,
    )
    assert "alertas_por_tier" in result, "Campo alertas_por_tier ausente na raiz do summary"
    tier = result["alertas_por_tier"]
    assert isinstance(tier, dict), "alertas_por_tier deve ser dict"
    assert tier.get("ATENÇÃO") == 2, f"Esperado ATENÇÃO=2, obtido {tier.get('ATENÇÃO')}"
    assert tier.get("CRÍTICO") == 1, f"Esperado CRÍTICO=1, obtido {tier.get('CRÍTICO')}"
    assert tier.get("SEM_REFERÊNCIA") == 1, f"Esperado SEM_REFERÊNCIA=1, obtido {tier.get('SEM_REFERÊNCIA')}"

def test_cobertura_bps_df_vazio(self, tmp_path):
    """DataFrame vazio (arquivo ausente) → cobertura_bps_pct=0.0 e alertas_por_tier={}."""
    out = tmp_path / "summary.json"
    exporter = Exporter()
    result = exporter.gerar_summary(
        alertas_file=str(tmp_path / "inexistente.csv"),
        output_file=str(out),
        ano=2023,
    )
    assert result.get("cobertura_bps_pct") == 0.0, "DataFrame vazio deve retornar cobertura_bps_pct=0.0"
    assert result.get("alertas_por_tier") == {}, "DataFrame vazio deve retornar alertas_por_tier={}"
    assert result.get("total_itens") == 0, "DataFrame vazio deve retornar total_itens=0"
```

Após adicionar os métodos, executar:
```bash
python -m pytest tests/test_exporter.py::TestGerarSummary -x -q
```
Os quatro novos testes devem FALHAR (RED) pois os campos ainda não existem em gerar_summary().
Os testes existentes devem PASSAR.
  </action>

  <verify>
    <automated>
python -m pytest tests/test_exporter.py::TestGerarSummary::test_total_itens tests/test_exporter.py::TestGerarSummary::test_cobertura_bps_pct tests/test_exporter.py::TestGerarSummary::test_alertas_por_tier tests/test_exporter.py::TestGerarSummary::test_cobertura_bps_df_vazio -v 2>&1 | grep -E "FAILED|ERROR" | head -5
    </automated>
  </verify>

  <done>
tests/test_exporter.py contém os 4 novos métodos em TestGerarSummary. Os 4 novos testes falham com AttributeError ou AssertionError (RED confirmado). Os testes existentes de TestGerarSummary passam sem regressão.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: Implementar os 3 novos campos em gerar_summary() — GREEN</name>

  <read_first>
    src/utils/exporter.py
    tests/test_exporter.py
  </read_first>

  <behavior>
    - gerar_summary() retorna dict com "total_itens" (int) na raiz
    - gerar_summary() retorna dict com "cobertura_bps_pct" (float, arredondado a 1 casa) na raiz
    - gerar_summary() retorna dict com "alertas_por_tier" (dict de str→int) na raiz
    - DataFrame vazio → total_itens=0, cobertura_bps_pct=0.0, alertas_por_tier={}
    - Os campos existentes em "totais" NÃO devem ser alterados (sem regressão em test_schema_totais)
    - Os novos campos ficam na RAIZ do dict (mesmo nível de "totais", "gerado_em", "municipio")
  </behavior>

  <action>
Editar `src/utils/exporter.py`, método `gerar_summary()`.

MUDANÇA 1 — Adicionar cálculo de cobertura APÓS o cálculo de `distribuicao` (linha ~213).
Inserir este bloco ANTES da criação do dict `summary`:

```python
# Cálculo de cobertura BPS e contagem por tier (STAB-07)
total_itens = len(alertas_df)
if not alertas_df.empty and "nivel_alerta" in alertas_df.columns:
    sem_ref = int((alertas_df["nivel_alerta"] == "SEM_REFERÊNCIA").sum())
    com_bps = total_itens - sem_ref
    cobertura_bps_pct = round((com_bps / total_itens) * 100, 1) if total_itens > 0 else 0.0
    alertas_por_tier: dict = {
        k: int(v) for k, v in alertas_df["nivel_alerta"].value_counts().items()
    }
else:
    cobertura_bps_pct = 0.0
    alertas_por_tier = {}
```

MUDANÇA 2 — Adicionar os 3 novos campos na RAIZ do dict `summary`.
No bloco `summary = { ... }`, adicionar APÓS a chave "distribuicao_alertas" (última chave existente):

```python
        "total_itens": total_itens,
        "cobertura_bps_pct": cobertura_bps_pct,
        "alertas_por_tier": alertas_por_tier,
```

O dict summary completo ficará assim (estrutura final):
```python
summary = {
    "gerado_em": datetime.now().isoformat(),
    "municipio": "Sorocaba",
    "ano": ano,
    "periodo_analise": {"inicio": inicio, "fim": fim},
    "totais": {
        "empenhos_analisados": total_empenhos,
        "alertas_atencao": alertas_atencao,
        "alertas_alerta": alertas_alerta,
        "alertas_criticos": alertas_criticos,
        "valor_total_excedente": valor_total,
        "fornecedores_suspeitos": qtd_fornecedores_suspeitos,
    },
    "top_itens": top_itens,
    "top_fornecedores": top_fornecedores,
    "distribuicao_alertas": distribuicao,
    "total_itens": total_itens,
    "cobertura_bps_pct": cobertura_bps_pct,
    "alertas_por_tier": alertas_por_tier,
}
```

IMPORTANTE: A variável `total_empenhos` já existe (linha ~133) e representa `len(alertas_df)`.
Para evitar duplicação, a variável `total_itens` no novo bloco pode reusar `total_empenhos`:
`total_itens = total_empenhos` — ambas têm o mesmo valor.

Não remover nem alterar o bloco de `distribuicao` existente. Não alterar "totais".

Após as mudanças, executar os testes para confirmar GREEN:
```bash
python -m pytest tests/test_exporter.py::TestGerarSummary -x -q
```
Todos os testes da classe devem passar (os 6 existentes + os 4 novos = 10 testes).
  </action>

  <verify>
    <automated>
python -m pytest tests/test_exporter.py::TestGerarSummary -v -q 2>&1 | tail -5
    </automated>
  </verify>

  <done>
src/utils/exporter.py:gerar_summary() retorna dict com os campos "total_itens" (int), "cobertura_bps_pct" (float) e "alertas_por_tier" (dict) na raiz. python -m pytest tests/test_exporter.py::TestGerarSummary -v retorna 10 passed (6 existentes + 4 novos), 0 failed.
  </done>
</task>

<task type="auto">
  <name>Tarefa 3: Expandir bloco de resumo em main.py com linha BPS match e WARNING</name>

  <read_first>
    main.py
    src/utils/exporter.py
  </read_first>

  <action>
Editar `main.py`. Substituir o bloco de resumo no terminal (linhas 184-195) pela versão expandida.

LOCALIZAR o bloco atual (identificável por `# Mostrar resumo no terminal`):
```python
    # Mostrar resumo no terminal (lê de docs/summary.json se disponível)
    summary_file = ROOT / "docs" / "summary.json"
    if not summary_file.exists():
        summary_file = output_path / "summary.json"
    if summary_file.exists():
        with open(summary_file) as f:
            s = json.load(f)
        totais = s.get("totais", {})
        console.print(f"\n[bold]Resumo:[/bold]")
        console.print(f"   Empenhos analisados: {totais.get('empenhos_analisados', '?')}")
        console.print(f"   Alertas CRÍTICOS: {totais.get('alertas_criticos', '?')}")
        console.print(f"   Valor total excedente: R$ {totais.get('valor_total_excedente', '?')}")
```

SUBSTITUIR por este bloco expandido (mantendo a lógica de localização do arquivo intacta):
```python
    # Mostrar resumo no terminal (lê de docs/summary.json se disponível)
    summary_file = ROOT / "docs" / "summary.json"
    if not summary_file.exists():
        summary_file = output_path / "summary.json"
    if summary_file.exists():
        with open(summary_file) as f:
            s = json.load(f)
        totais = s.get("totais", {})
        cobertura = s.get("cobertura_bps_pct", 0.0)
        no_match = round(100.0 - cobertura, 1)
        n = s.get("total_itens", totais.get("empenhos_analisados", "?"))
        console.print(f"\n[bold]Resumo:[/bold]")
        console.print(f"   Empenhos analisados: {totais.get('empenhos_analisados', '?')}")
        console.print(f"   Alertas CRÍTICOS: {totais.get('alertas_criticos', '?')}")
        console.print(f"   Valor total excedente: R$ {totais.get('valor_total_excedente', '?')}")
        console.print(f"   Items: {n} | BPS match: {cobertura:.1f}% | No match: {no_match:.1f}%")
        if isinstance(cobertura, (int, float)) and cobertura < 50:
            console.print(
                "[bold yellow][WARNING][/bold yellow] "
                f"Cobertura BPS {cobertura:.1f}% abaixo de 50% — "
                "análise de preços pouco representativa para este conjunto de dados."
            )
```

Não alterar nenhuma outra parte do arquivo. O `json` já está importado no topo de main.py.

Após a mudança, verificar sintaxe:
```bash
python -c "import ast; ast.parse(open('main.py').read()); print('syntax OK')"
```
  </action>

  <acceptance_criteria>
grep -c "BPS match" main.py | grep -q "^1$"
grep -c "cobertura_bps_pct" main.py | grep -q "^1$"
grep -c "bold yellow.*WARNING" main.py | grep -q "^1$"
python -c "import ast; ast.parse(open('/home/richizo/Projects/me/civisbr/medicamentos-sus-sorocaba-sp/main.py').read()); print('OK')"
  </acceptance_criteria>

  <verify>
    <automated>
cd /home/richizo/Projects/me/civisbr/medicamentos-sus-sorocaba-sp && python -c "import ast; ast.parse(open('main.py').read()); print('syntax OK')" && grep "BPS match" main.py && grep "WARNING" main.py && python -m pytest tests/ -q 2>&1 | tail -3
    </automated>
  </verify>

  <done>
main.py contém exatamente 1 ocorrência de "BPS match" e 1 ocorrência de "[bold yellow][WARNING][/bold yellow]". python -c com ast.parse retorna "syntax OK". python -m pytest tests/ -q passa com zero regressões (todos os testes existentes continuam verdes).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| filesystem → gerar_summary() | CSV de alertas lido de disco e processado pelo pandas |
| summary.json → main.py | JSON lido de disco e printado no terminal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Tampering | summary.json em docs/ | accept | Arquivo gerado pelo pipeline a partir de dados públicos; sem credenciais ou PII; qualquer adulteração é detectável comparando com dados brutos |
| T-06-02-02 | Denial of Service | gerar_summary() com DataFrame grande | accept | O cálculo de cobertura usa operações pandas vetorizadas (`.sum()`, `.value_counts()`) — O(n) sobre coluna única; sem risco de OOM para volumes de dados municipais |
| T-06-02-03 | Information Disclosure | console.print com cobertura_bps_pct | accept | Métrica de qualidade de dados públicos; sem dados pessoais expostos na saída do terminal |
</threat_model>

<verification>
Após completar todas as tarefas, executar em sequência:

```bash
cd /home/richizo/Projects/me/civisbr/medicamentos-sus-sorocaba-sp

# 1. Novos testes da Tarefa 1+2 passando
python -m pytest tests/test_exporter.py::TestGerarSummary -v
# Esperado: 10 passed (6 existentes + 4 novos)

# 2. Campos corretos em exporter.py
grep -c "total_itens" src/utils/exporter.py      # deve retornar >= 2 (cálculo + no dict)
grep -c "cobertura_bps_pct" src/utils/exporter.py  # deve retornar >= 2
grep -c "alertas_por_tier" src/utils/exporter.py   # deve retornar >= 2

# 3. main.py expandido
grep "BPS match" main.py   # deve retornar 1 linha
grep "WARNING" main.py     # deve retornar 1 linha
python -c "import ast; ast.parse(open('main.py').read()); print('syntax OK')"

# 4. Nenhuma regressão
python -m pytest tests/ -q
# Esperado: todos os testes passando (90+ testes)
```
</verification>

<success_criteria>
1. `python -m pytest tests/test_exporter.py::TestGerarSummary -v` retorna 10 passed, 0 failed
2. `grep -c "total_itens" src/utils/exporter.py` retorna >= 2
3. `grep -c "cobertura_bps_pct" src/utils/exporter.py` retorna >= 2
4. `grep -c "alertas_por_tier" src/utils/exporter.py` retorna >= 2
5. `grep "BPS match" main.py` retorna 1 linha com o formato `Items: {n} | BPS match:`
6. `grep "bold yellow.*WARNING" main.py` retorna 1 linha
7. `python -c "import ast; ast.parse(open('main.py').read()); print('OK')"` retorna "OK"
8. `python -m pytest tests/ -q` passa com zero regressões
</success_criteria>

<output>
Após completar todas as tarefas, criar:
  .planning/phases/06-validation-data-quality/06-02-SUMMARY.md

O SUMMARY deve registrar:
- Campos adicionados ao summary.json (com estrutura exata)
- Linha de resumo adicionada ao main.py (com formato exato)
- Resultado de cada critério de sucesso
- Quaisquer desvios em relação ao plano
</output>
